package day45_Inheritance;

public class protectedAccessModifier {
	/*
	 * protected: only visible to the classess that are in the same package
	 * but can be inheritable to the classess outside the package
	 * 
	 * Not visible but Inheritable
	 */

}
